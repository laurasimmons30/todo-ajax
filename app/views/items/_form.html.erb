<div class="row">  
  <div class="large-12">
    <%= form_for @item, remote: true do |f| %>
      <%= f.label :title %>
      <%= f.text_field :title %>

      <%= f.label :description %>
      <%= f.text_area :description %>

      <%= f.submit "Add Item" %>
    <% end %>
  </div>
</div>
